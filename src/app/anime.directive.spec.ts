import { AnimeDirective } from './anime.directive';

describe('AnimeDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimeDirective();
    expect(directive).toBeTruthy();
  });
});
